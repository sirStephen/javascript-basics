// 1. You cant define a variable more than once
let petName = 'Black';
petName = 'Cruffy'

console.log(petName);

// 2. There are rules related to variable names
// All variable names must start with small letters. You cannot start a variable name with a number. 
let test_ = 2;
let result = 3 + 4;

// 3. Variables names cannot be reserved words example
let let = "Gregory"; // going to get an error.
